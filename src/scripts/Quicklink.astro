<script client:idle src="https://cdnjs.cloudflare.com/ajax/libs/quicklink/2.2.0/quicklink.umd.js">
</script>
  <script defer>
    quicklink.listen();

    /* ---- Full default configurations ---- */
    // quicklink.listen({
    //   el: document.body,
    //   delay: 0,
    //   limit: Infinity,
    //   throttle: Infinity,
    //   timeout: Infinity,
    //   timeoutFn: requestIdleCallback,
    //   priority: false,
    //   origins: [location.hostname],
    //   //! .origins: An empty array ([]) allows all origins to be prefetched.
    //   ignores: [],
    //   onError: () => {},
    // })

    // quicklink.prefetch([...urlStrings], false : boolean)
  </script>